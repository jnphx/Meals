@page
@model WeeklyMeals.Pages.MealPlans.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>MealPlan</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.MealPlan.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.MealPlan.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.MealPlan.MealPlanRecipes)
        </dt>
        <dd class="col-sm-10">
            <table class="table">
                <tr>
                    <th>Recipe</th>
                </tr>
                @foreach (var item in Model.MealPlan.MealPlanRecipes)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Recipe.Name)
                        </td>
                    </tr>
                }
            </table>
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.MealPlan.MealPlanID">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
